{# templates/contact/contact.html.twig #}
{% extends 'base.html.twig' %}


{% block body %}
<style>
    .contact-container {
        max-width: 500px;
        margin: 50px auto;
        padding: 40px;
        background: #fff;
        border-radius: 20px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        font-family: 'Arial', sans-serif;
        text-align: center;
    }

    .contact-container h1 { color: #064523; margin-bottom: 10px; }
    .contact-container p { color: #6bb16b; margin-bottom: 30px; font-weight: bold; }

    .form-group { margin-bottom: 20px; text-align: left; }
    .form-group label { display: block; margin-bottom: 5px; color: #333; font-weight: bold; }

    .form-control {
        width: 100%;
        padding: 15px;
        border: 2px solid #6bb16b;
        border-radius: 15px;
        background-color: #f0f7ff;
        box-sizing: border-box;
        font-size: 16px;
    }

    .btn-submit {
        width: 100%;
        padding: 15px;
        background-color: #387d38;
        color: white;
        border: none;
        border-radius: 10px;
        font-size: 18px;
        font-weight: bold;
        cursor: pointer;
        margin-top: 20px;
    }

    .btn-submit:hover { background-color: #2d632d; }
    .footer-text { margin-top: 20px; font-size: 12px; color: #666; }
</style>
<div class="contact-container">
    <h1>Contactez-nous</h1>
    <p>Nous sommes à votre écoute pour un avenir meilleur</p>
     {% for message in app.flashes('success') %}
        <div class="alert alert-success">{{ message }}</div>
    {% endfor %}

    {{ form_start(form) }}
        <div class="form-group">
            <label>Votre nom</label>
            {{ form_widget(form.nom, {'attr': {'class': 'form-control', 'placeholder': 'Votre nom'}}) }}
        </div>

        <div class="form-group">
            <label>Votre e-mail</label>
            {{ form_widget(form.email, {'attr': {'class': 'form-control', 'placeholder': 'Votre e-mail'}}) }}
        </div>

        <div class="form-group">
            <label>Sujet</label>
            {{ form_widget(form.subject, {'attr': {'class': 'form-control', 'placeholder': 'Sujet'}}) }}
        </div>

        <div class="form-group">
            <label>Votre message</label>
            {{ form_widget(form.message, {'attr': {'class': 'form-control', 'placeholder': 'Votre message', 'rows': '4'}}) }}
        </div>

        {{ form_start(form) }}
            <button type="submit" class="btn-submit">Envoyer le message</button>
        {{ form_end(form) }}
    {{ form_end(form) }}

    <div class="footer-text">
        En envoyant ce message, vous acceptez notre <br>
        <a href="#" style="color: #387d38;">politique de confidentialité</a>.
    </div>
</div>
{% endblock %}